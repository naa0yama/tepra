//! Application-specific OTel semantic convention constants.
//!
//! This module mirrors the structure of [`opentelemetry_semantic_conventions`] for
//! `tepra.*` custom metric and attribute names. Upstream semconv constants are used
//! directly from that crate; only names outside the upstream spec are defined here.
//!
//! Add `tepra.printer.*` metric names in Cycle 10 when the printer actor metrics
//! are instrumented.
