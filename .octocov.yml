# generated by octocov init
comment:
  deletePrevious: true
  if: is_pull_request
  hideFooterLink: true
codeToTestRatio:
  code:
    - "**/*.rs"
    - "!**/tests/**/*.rs"
    - "!**/*test*.rs"
  test:
    - "**/tests/**/*.rs"
    - "**/*test*.rs"
coverage:
  acceptable: current >= 90%
  badge:
    path: coverage.svg
  if: true
  paths:
    - target/lcov.info
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
summary:
  if: true
  hideFooterLink: true
testExecutionTime:
  badge:
    path: time.svg
  if: true
